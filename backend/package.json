{
  "name": "pizzeria-search-backend",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "convex dev",
    "build": "echo 'Convex is serverless; no build step'",
    "deploy": "convex deploy"
  },
  "dependencies": {
    "convex": "^1.15.0"
  },
  "devDependencies": {
    "@types/node": "^20.11.30",
    "typescript": "^5.2.2"
  }
}
